version: '3.8'

services:
  bot:
    build: .
    container_name: siyanie_neba_bot
    restart: always
    environment:
      - TZ=Europe/Moscow
    volumes:
      - ./data:/app/data
    env_file:
      - .env
